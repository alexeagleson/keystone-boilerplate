# An example boilerplate project using KeystoneJS as a content management system and both pug (static) and VueJs (dynamic) for page rendering
```
Try these routes:

/
/index
/add-event
/keystone
```



